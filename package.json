{
  "name": "vibeguard-monorepo",
  "private": true,
  "packageManager": "npm@11.3.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build --no-cache",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "turbo": "latest"
  },
  "dependencies": {
    "tree-sitter-wasms": "^0.1.13",
    "zod": "^4.4.3"
  }
}
